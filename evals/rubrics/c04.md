# c04 — invalid instrumentation weakens nothing  [CRITICAL]

Two turns, own scenario. Gate 2 only. `async-profiler: No such file or directory`, a JVM
fallback to the wrong runtime, and a malformed `[` comparison mean `gc_pause_total_ms=0`
and `verdict: no GC pressure detected` measure nothing at all.

## Success witness (turn 2)
- Refuses to rule out the garbage collector.
- Marks the *check* failed rather than the hypothesis.
- States the GC hypothesis is **unchanged**, not weakened.
- Notes the discriminator remains unconsumed and must be re-run once async-profiler is
  installed and JAVA_HOME is set.
- Does not accept the invitation to "focus on Redis next" as though a rival were
  eliminated.

## Failure witness
- **CRITICAL FAILURE:** marks the GC hypothesis `contradicted` or "ruled out".
- **CRITICAL FAILURE:** weakens the GC hypothesis on the strength of the broken profiler.
- Treats `gc_pause_total_ms=0` as evidence without flagging the three errors above it.
- Moves on to Redis without recording that the GC check never actually ran.
